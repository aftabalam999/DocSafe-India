{
    "name": "docsafe-india",
    "version": "1.0.0",
    "description": "Secure & Share Government Documents with Family (MERN Stack)",
    "scripts": {
        "install:all": "npm install && cd backend && npm install && cd ../frontend && npm install",
        "dev:backend": "cd backend && npm run dev",
        "dev:frontend": "cd frontend && npm run dev",
        "main": "npm run dev:backend & npm run dev:frontend"
    },
    "devDependencies": {
        "concurrently": "^8.2.2"
    }
}